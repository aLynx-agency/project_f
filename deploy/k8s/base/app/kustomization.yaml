apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - migrate-job.yaml

labels:
  - pairs:
      app.kubernetes.io/name: project-f
    includeSelectors: true
